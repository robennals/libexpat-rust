# Known failing C tests - these are expected to fail.
# Remove tests from this list as they are fixed.
# CI will fail if any test NOT in this list starts failing (regression).
#
# Categories:
# [principled-skip] - C-specific functionality that doesn't apply to Rust

# Principled skips - these will never be fixed (C-specific allocator internals)
test_accounting_precision                          # [principled-skip] C-internal g_bytesScanned testing counter
test_misc_alloc_create_parser                      # [principled-skip] Custom C malloc/realloc/free
test_misc_alloc_create_parser_with_encoding        # [principled-skip] Same with encoding parameter
