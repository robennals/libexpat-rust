{
  "description": "TEMPORARY rewrite rules for patterns we believe are equivalent but haven't fully verified. Each rule should be reviewed and either promoted to structural-rewrites.json (if confirmed correct) or removed (if found to be a real bug). Track these carefully.",
  "version": 1,
  "created": "2026-03-26",

  "temporary_rules": [
    {
      "name": "report_call_return_check_to_void",
      "input":  { "kind": "branch", "label_regex": "!\\s*report_(comment|processing_instruction|default)" },
      "output": null,
      "justification": "C checks return value of reportComment/reportPI/reportDefault for memory allocation failure (returns 0 on OOM). Rust report_* functions return void because Rust panics on OOM. TEMPORARY: this assumes OOM panic is acceptable behavior.",
      "risk": "If Rust ever needs graceful OOM handling, these checks would need to be restored.",
      "status": "believed_equivalent"
    },
    {
      "name": "must_convert_encoding_branch",
      "input":  { "kind": "branch", "label_regex": "MUST_CONVERT|must_convert" },
      "output": null,
      "justification": "C checks MUST_CONVERT(enc, s) to decide if character encoding conversion is needed before passing data to handlers. Rust assumes UTF-8 only, so no conversion is ever needed. TEMPORARY: this is correct for UTF-8 input only.",
      "risk": "If multi-encoding support is added, encoding conversion logic would need to be ported.",
      "status": "believed_equivalent"
    },
    {
      "name": "handler_check_to_if_let",
      "input":  { "kind": "branch", "label_regex": "^!?\\s*(character_data_handler|char_data_handler|default_handler|start_element_handler|end_element_handler|end_cdata_section_handler|start_cdata_section_handler|comment_handler|processing_instruction_handler|skipped_entity_handler|xml_decl_handler|start_doctype_decl_handler|end_doctype_decl_handler|notation_decl_handler|entity_decl_handler|attlist_decl_handler|element_decl_handler|unparsed_entity_decl_handler|external_entity_ref_handler|not_standalone_handler|start_namespace_decl_handler|end_namespace_decl_handler)" },
      "output": { "kind": "branch", "label_regex": "let Some.*handler" },
      "justification": "C checks handler pointer: if (handler) { handler(arg, data) }. Rust uses if-let: if let Some(handler) = &mut self.*_handler { handler(data) }. Both check the same handler and dispatch the same way. TEMPORARY: believed equivalent but the nested structures may differ.",
      "risk": "Handler dispatch details (arguments, fallback to default handler) may differ within the branch body.",
      "status": "believed_equivalent"
    },
    {
      "name": "parsing_status_final_buffer_to_is_final",
      "input":  { "kind": "branch", "label_regex": "parsing_status\\.final_buffer|!parsing_status" },
      "output": { "kind": "branch", "label_regex": "is_final|have_more" },
      "justification": "C checks !parsing_status.finalBuffer for incomplete input handling. Rust uses have_more (= !self.is_final). Both determine if more data may arrive. TEMPORARY: the variable name mapping is clear but specific behavior on final buffer may differ.",
      "risk": "Suspend/resume semantics not fully ported.",
      "status": "believed_equivalent"
    },
    {
      "name": "parsing_status_finished_check",
      "input":  { "kind": "branch", "label_regex": "parsing_status.*FINISHED|parsing.*==.*XML_FINISHED" },
      "output": null,
      "justification": "C checks if parsing is finished (XML_PARSING_FINISHED) for suspend/resume support. Rust doesn't support suspend/resume. TEMPORARY: safe to omit only if suspend/resume is never used.",
      "risk": "If suspend/resume is ported, this check must be added.",
      "status": "believed_equivalent"
    },
    {
      "name": "empty_sequence_in_arm",
      "input":  { "kind": "sequence", "children_count": 0 },
      "output": null,
      "justification": "Empty sequences appear from C code blocks that only contain suppressed operations (pool ops, position tracking). TEMPORARY: should verify each case individually.",
      "risk": "May mask a missing operation.",
      "status": "believed_equivalent"
    },
    {
      "name": "default_case_arm",
      "input":  { "kind": "arm", "label": "_default" },
      "output": null,
      "justification": "C default: case typically returns XML_ERROR_UNEXPECTED_STATE. Rust wildcard _ arm may have different handling. TEMPORARY: Rust's catch-all behavior should be verified per-function.",
      "risk": "Default case error handling may differ.",
      "status": "believed_equivalent"
    },
    {
      "name": "wrapper_processor_delegating_call",
      "input":  { "kind": "return", "label_regex": "do[Pp]rolog|do[Cc]ontent|do_prolog|do_content|externalEntityInitProcessor|external_entity_init_processor" },
      "output": null,
      "justification": "C processor wrappers (prologProcessor, contentProcessor) return the result of calling the do* function directly. Rust inlines this differently. TEMPORARY: believed equivalent because the do* function is still called.",
      "risk": "Post-call processing (like storeRawNames) may be missing.",
      "status": "believed_equivalent"
    },
    {
      "name": "result_error_check_after_call",
      "input":  { "kind": "branch", "label_regex": "result\\s*[!=]=\\s*(XML_ERROR_NONE|XmlError::None|0)" },
      "output": null,
      "justification": "C checks result == XML_ERROR_NONE after calling a function to decide if post-processing is needed. Rust may check the error differently (match, if, or inline). TEMPORARY: the error check pattern differs but the semantics should be equivalent.",
      "risk": "Post-success processing may be missing in Rust.",
      "status": "believed_equivalent"
    }
  ]
}
